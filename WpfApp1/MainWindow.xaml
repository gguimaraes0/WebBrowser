<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" WindowStyle="ThreeDBorderWindow" WindowStartupLocation="CenterScreen" WindowState="Maximized" ShowActivated="True">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition Height="90*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Background="Gray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="88*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button
                  Click="btnVoltar_Click"
               Padding="5"
                Height="30"
               Grid.Column="0"
               Content="{materialDesign:PackIcon ArrowLeft}"
               ToolTip="Icon"
               IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
            <Button
               Click="btnAvancar_Click"
               Height="30"
               Grid.Column="1"
               Content="{materialDesign:PackIcon ArrowRight}"
               ToolTip="Icon"
               IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
            <Button
                Height="30"
               Padding="5"           
               Grid.Column="2"
               Click="btnRefresh_Click"
               Content="{materialDesign:PackIcon Refresh}"
               ToolTip="Icon"
               IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />

            <TextBox Grid.Column="3" x:Name="txtUrl" FontSize="18" TextChanged="txtUrl_TextChanged" VerticalAlignment="Center" Height="30" KeyDown="txtUrl_KeyDown">
            </TextBox>
            <Button             
               Padding="5"  
                Height="30"
               Grid.Column="4"
               Content="{materialDesign:PackIcon DownloadOutline}"
               ToolTip="Icon"
               IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
            <Button
                Height="30"
               Padding="5"                
               Grid.Column="5"
               Content="{materialDesign:PackIcon SettingsHelper}"
               ToolTip="Icon"
               IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />

        </Grid>
        <Grid Grid.Row="2" x:Name="gridContent">
        </Grid>-->

    </Grid>
</Window>
